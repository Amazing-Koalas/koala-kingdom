import { Component } from "./Component";
import { GameEngine } from "./GameEngine";

export class Sound extends Component {
    load(game: GameEngine) {
        return this;
    }

    render(game: GameEngine): void {
    }
}